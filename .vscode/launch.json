{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome",
        "request": "launch",
        "name": "Launch Chrome",
        "url": "http://localhost:4100", // 修改为你的本地服务地址
        "webRoot": "${workspaceFolder}/src"
      }
    ]
  }